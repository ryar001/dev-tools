[project]
name = "dev-mcp"
version = "0.1.0"
description = "A command-line tool to run the ai-tracker.sh script."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp",
    "pip>=25.3",
    "ruff",
]

[project.scripts]
dev-mcp = "dev_mcp:main"
